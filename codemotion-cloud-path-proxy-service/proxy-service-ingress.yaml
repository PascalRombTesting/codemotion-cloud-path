apiVersion: v1
kind: Ingress
metadata:
  name: shop-ingress
  annotations:
    ingress.kubernetes.io/ssl-redirect: "false"
spec:
  rules:
    - host: localhost
      http:
        paths:
        - path: /
          backend:
            service:
              name: proxy-service-lb
              port:
                number: 80